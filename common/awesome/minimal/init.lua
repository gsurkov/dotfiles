-- An AwesomeWM library so minimal it is barely useful.
-- Heavily inspired by lain widgets: https://github.com/lcpz/lain

return {
    widget = require("minimal.widget"),
    util = require("minimal.util"),
}
